import { atomFamily, selectorFamily } from "recoil";

/*
    Create an atom family
    create a selector family
    create the async fetching for the selector family
    create the single blog card template
    fill the template using map function
*/
